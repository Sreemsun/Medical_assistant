services:
  - type: web
    name: medical-assistant
    env: node
    plan: free
    buildCommand: npm install --prefix server && pip install -r ml/requirements.txt
    startCommand: npm start --prefix server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PYTHON_EXECUTABLE
        value: python3
